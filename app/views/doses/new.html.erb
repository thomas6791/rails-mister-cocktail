<h1>Créer une dose</h1>

<div class="container">
  <div class="row">
    <div class="col-xs-offset-4 col-xs-4 text-center">
      <%= simple_form_for [@cocktail, @dose] do |f| %>
        <%= f.input :description %>
        <%= f.input :ingredient_id, collection: Ingredient.all %>
        <%= f.button :submit, class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>
